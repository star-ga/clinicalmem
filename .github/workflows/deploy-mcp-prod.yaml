name: Deploy MCP Server (prod)
on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    uses: ./.github/workflows/deploy-env.yaml
    with:
      APP_NAME: clinicalmem-mcp
      DOCKERFILE: Dockerfile.mcp
    secrets:
      AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
